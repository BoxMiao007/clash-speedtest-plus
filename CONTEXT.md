# Clash Speedtest

测速一次订阅或配置里的代理节点，按延迟、丢包、下载/上传速度筛选，并在终端里观察测速过程。

## Language

**节点并行**:
同时对多个节点各自跑完整测速。这是用户要的「一次测几个节点」，与单节点内部的多连接无关。同时在测的节点数不超过 `-p` / `-parallel`；暂停后再继续时等有空位再开新节点，不额外加塞。派发前按节点名排序，同一份名单每次测同一批。
_Avoid_: 并发测速（单独使用时）、concurrent

**下载并发**:
对同一个节点的下载（或上传）同时开多条连接。现有 `-concurrent` 只表示这个。
_Avoid_: 节点并行、同时测 N 个节点

**在测行**:
表格里尚未完成的节点：已开始测，还没有最终结果。排在已完成行下面，不参与点表头排序。序号列写 …。延迟阶段相关列写「测试中」；延迟一结束就填真实延迟/抖动/丢包；下载/上传进行中显示该阶段的瞬时速度，已结束的阶段列定格。终端里在测行文字是灰色，选中时用和完成行相同的亮底。行数超出窗口时，滚动条贴在窗口最右一列；放得下时不显示。点轨道空白处或拖动滑块只滚动视口，选中行不变，也不打开详情。点数据行按画面上那一行选中并打开详情；进度行被终端折行时仍按折行后的位置判断，点表头才改排序。打开详情不把列表滚走。结果表图里在测行同样排在完成行下面，用灰色，不走阈值配色。点在测行打开测试中占位详情；再点同一行则关掉。详情打开后方向键和滚轮仍可滚动，详情跟着当前行走。该节点测完后，选中行和详情跟着这条结果走进完成区。
_Avoid_: 预览、测试预览、干跑名单

**占位详情**:
点在测行打开的详情面板：展示该节点当前已有的延迟/瞬时速度，并标明仍在测试中。随在测行一起刷新；该节点测完后自动切成与点完成行相同的完整详情。
_Avoid_: 完成详情

**瞬时速度**:
在测行上该阶段已传输字节除以该阶段已用时间（累计平均），越测越接近最终带宽。下载进行中填下载列；下载结束后该列定格，上传进行中再填上传列。延迟阶段没有瞬时速度。约每 200ms 刷新一次。
_Avoid_: 平均速度、预估速度、窗口网速、完成后的带宽

**结果表图**:
把当前结果表渲染成浅色干净 PNG（白底、表头、斑马纹），用于转发：含排在完成行下面的在测行（若有）和全部已测节点，不过筛。节点名用订阅原名，不跟 `-rename` 后的 yaml 名称。完成行各指标列的颜色深浅按本批结果该列的最小到最大值独立归一化（见 ADR-0008），上传列不被下载列稀释；在测行灰色。图顶来源行加摘要行。交互模式按当前表排序；非交互自动导出按与 TUI 完成后相同的默认排序。正文字号 15。节点名列按最长名字撑宽，并额外留一截；节点名超过 64 个字符时才截断并加省略号。类型、延迟、抖动、丢包率按单元格原文撑开。下载速度、上传速度里的错误文本不撑列宽，绘制时再截断。交互模式下测速中、暂停中、结束后可按 `s` 手动导出（`-no-image` 关不掉手动）；编码用按下/触发那一瞬的快照，已有一张正在编码时再按 `s` 忽略，状态行提示「正在保存」。整轮测完立刻自动再写一张（交互后台编码不卡界面；非交互在退出前写完。可用 `-no-image` 关掉自动导出）。自动和手动保存后，TUI 状态行显示路径或失败原因约 3 秒再回到进度行；后到的状态行提示盖住前一条（含 gist/仓库结果）。非交互编码前 stderr 写「正在保存」，完成后写已保存路径。缺字体时每次出图都警告，并与保存成功拼成一句。写入当前目录、文件名带本地时间戳；同一秒冲突则加 `-1`、`-2` 序号，不覆盖。全量长图，高度不封顶。优先用系统 CJK/emoji 字体（含 Windows 的 Segoe UI Emoji、WSL 挂载路径），没有就降级。不是终端截图，也不是摘要卡。
_Avoid_: 截图、分享卡、海报、仿终端配色、覆盖同名图、yaml 重命名后的节点名

**超调**:
提前停止按「过筛结果数」计。限额到达后不再启动新节点，但已经在测的会跑完并照收，最终过筛数可能略超过限额。提前停止后空格不能再测剩下的节点。
_Avoid_: 截断在测结果、硬限额

**拓尾**:
已经开始测、限额或暂停之后仍要跑完的那些节点。拓尾期间进度行在提前结束时写「已提前结束」并带在测数。
_Avoid_: 立刻掐断、丢弃在测结果

**提前结束**:
因过筛数量达到 `-early-stop` 限额而结束本轮。限额一到进度行就写「已提前结束」并带在测数，不再显示剩余；拓尾收完后仍是「已提前结束」，不是「已完成」，也不是「已暂停」。空格从限额到达起就无效，帮助条不再显示空格。
_Avoid_: 已完成、已暂停

**整轮结束**:
全部测完或提前结束，且拓尾已收完。此时立刻写 `-output` yaml 并自动导出结果表图（除非 `-no-image`）。交互模式里 gist/仓库上传在后台进行，不卡住界面，离开时不等上传（进程结束会掐掉未完成的上传）。非交互模式等 gist/仓库成功或失败后再退出。写盘或出图失败时状态行报错，不杀进程；最终离开时退出码非 0。gist/仓库失败不影响退出码。yaml 和图都成功时状态行合并成一句。之后仍可翻表、按 `s`。
_Avoid_: 关界面才写 yaml

**摘要行**:
结果表图顶部的一行说明：时间、测速模式（快速 / 下载 / 完整）、状态（测试中 / 已暂停 / 已完成 / 已提前结束）、已完成/总数；「结果图只留有速度」开启时再补括号计数：有效、无效、测试中、未测试，某项为 0 省略。摘要行上方是来源行：勾选的配置文件名或订阅地址（去查询参数），超宽按图宽自动折行。不写过滤条件或完整命令。
_Avoid_: 完整命令行、订阅地址、token、-c、-f、-b、fast、download、full

**暂停**:
在交互式 TUI 里按空格暂时停止测速：不再启动新节点，已经在测的跑完（拓尾）并马上上表。进度行改为「已暂停」，并写出在测数；已用时和剩余冻结。帮助条改为「空格 继续」。在测行的瞬时速度仍随拓尾更新。详情开着时按空格仍是暂停，不是关详情。全部测完或限额到达后空格无效果，帮助条隐藏空格，只留 s 保存、q 退出、esc 关闭详情。再按空格继续时立刻恢复派发（有空位才开新节点），不必等全部拓尾结束。暂停期间不写 `-output` yaml；yaml 仍只在整轮结束（含提前结束）时写。非交互/管道模式没有暂停。
_Avoid_: 退出、取消本轮测速、立刻掐断在测节点

**退出**:
按 `q` 或 Ctrl+C 结束整个进程（不只关界面）。本轮尚未结束时立刻丢掉在测节点，yaml 和自动结果表图都不写；若有正在编码的 PNG，丢掉并删掉半截文件。整轮结束后第一次按 `q` 或 Ctrl+C：进度行改为「正在保存」，帮助条只留「再按 q 或 Ctrl+C 强制退出」，界面冻结（只接受再一次退出），等到本地 yaml、自动结果表图、以及正在编码的手动图都写完再走（不等后台 gist/仓库）。等待落盘时再按 `q` 或 Ctrl+C：立刻走；写完的文件保留，只删半截，退出码非 0。非交互写图或等 gist/仓库时，Ctrl+C 立刻中断：已写完的 yaml 保留，半截图删掉；等上传时 stderr 写「正在上传」。要先收尾请先暂停等到拓尾完，需要图请先按 `s`。
_Avoid_: 暂停、拓尾退出、关 TUI 后台继续测

**界面文案**:
用户能看见的文字用简体中文：`-h` 标题（用法：clash-speedtest [选项]）和全部 flag、报错、TUI 进度/帮助/详情、保存与缺字体提示。测试代码除外。
_Avoid_: 中英混排的进度行、Usage of

**本地产物提示**:
yaml 和结果表图的路径不进 stdout。TUI 用状态行，非交互用 stderr。正在保存/已保存时 yaml 与图合并成一句。离开 TUI 后再向 stderr 打一行已保存路径（失败则打错误），方便复制。非交互写 yaml/图失败时 stderr 报错、退出码非 0，不中途崩溃。
_Avoid_: stdout 打 save config file to

**进度行**:
TUI 顶部状态：测试中 / 已暂停 / 已完成 / 已提前结束 / 正在保存，以及完成数/总数、在测数、已用时、剩余。只有还打算继续派发新节点时才显示剩余；暂停时已用时和剩余冻结；已完成、已提前结束或正在保存不显示剩余。在测数为 0 时不写「0 在测」。
_Avoid_: Testing、Paused、Completed、Elapsed、ETA

**配置**:
本地的 Clash/Mihomo yaml，里面有要测的节点。选源界面只把它当测速源候选，不叫订阅。
_Avoid_: 订阅文件、本地订阅

**订阅地址**:
远程测速源的 URL。打开后得到的是订阅内容，不是本地配置。多条之间用「逗号加空格」或换行分开；不带空格的逗号仍是地址的一部分。空行忽略。
_Avoid_: 订阅链接、远程配置

**测速源**:
一次测速所依据的节点来源：一份配置，或一条订阅地址。选源界面一次可以选多个，一起测。命令行仍可用逗号把多个路径或地址放进同一次 `-c`。
_Avoid_: 订阅（统称本地和远程时）、配置（统称远程时）

**程序目录**:
程序文件所在的目录。选源界面只在这里找配置。结果图，以及相对路径的输出 yaml，也写到这里；命令行同样，不再写启动时的当前目录。绝对路径照写。`../` 按程序目录解析，允许写到上级。
_Avoid_: 当前目录、工作目录

**选源界面**:
不带任何参数启动时出现的界面：从程序目录勾选合格配置，和/或填入订阅地址，并在同一屏勾选、填写测速选项。本地配置与订阅地址可以混在同一次测速里。回车后进入测速界面；测速界面里按 Esc 返回这里，勾选、地址、选项全部保留。退出只认 Ctrl+C，q 和 Esc 都不退出。带任何参数（含 `-h`、`-v`）不进入这里，仍走命令行。
_Avoid_: 启动器、菜单、双击界面

**跟随滚动**:
测速界面里视口贴着列表尾部、跟着最新测试条目滚动的模式，默认开启。向上滚动（键盘、滚轮、拖滚动条）就取消，滚回底部自动恢复；点表头排序也取消并回顶部，让排序结果立刻可见。
_Avoid_: 跟随光标、自动翻页

**合格配置**:
程序所在目录里的一份 `.yaml`：能解开成 Clash/Mihomo 配置，并且 `proxies` 或 `proxy-providers` 里至少有一个节点。`.yml` 和其他文件不是合格配置。列表按文件名不区分大小写排序。能看见但没有节点、或 yaml 语法坏了的 `.yaml` 列出来且不能勾。
_Avoid_: 任意 yaml、订阅文件

**订阅规范化**:
把订阅内容变成 Clash/Mihomo 配置的顺序：先当 yaml；不行则整段 base64 解码后再当 yaml；还不行且地址里没有 `flag` 参数时，用补了 `flag=meta` 的地址再请求，并重复前两步。解开后只要 `proxies` 或 `proxy-providers` 里至少有一项就停。不认分享链接，也不认 JSON 订阅。选源界面和命令行同一套。补过的地址要告诉用户，不改用户原来写的地址。拉取请求按 User-Agent 分流（见 ADR-0006）：选源默认 `clash.meta`，可用「拉取订阅 UA」覆盖；地址里看不见的控制字符在输入和切分两层剥掉。
_Avoid_: 自动改地址、静默换一份订阅、分享链接转换
