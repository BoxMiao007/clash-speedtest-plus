# 结果图和相对路径的输出写到程序目录

选源界面只扫描程序文件所在目录里的配置。若结果图和相对路径的 `-o` 仍写启动时的当前目录，双击、快捷方式和 `go run` 会把测速源和产物拆到两个地方。因此无论从选源界面还是命令行启动，结果图、以及相对路径的输出 yaml，都锚在程序文件所在目录。绝对路径照写。`../` 按程序目录解析，允许写到上级。

这改掉了「写当前目录」的旧行为，现有依赖 cwd 的脚本需要改路径。
