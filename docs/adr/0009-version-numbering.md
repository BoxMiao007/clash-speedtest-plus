# 版本号满十进位：次版本 0~9，1.9 之后是 2.0.0

本仓库的版本号规则是「每 10 进 1」，不是语义化版本的常规惯例：次版本号只从 0 用到 9，1.9 就是这一系的最后一个次版本，下一个大版本直接是 2.0.0，不出现 1.10。

具体到本仓库：v1.9.0 已发布，所以选源界面这批大功能发 **v2.0.0**。以后每个大版本同理：2.0~2.9，然后 3.0.0。

补丁号（第三段）不受此规则限制，照常从 0 递增。
